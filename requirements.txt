# Using py-cord instead of discord.py - modern fork without audioop dependencies
# py-cord is compatible with Python 3.13 and doesn't require audioop module
py-cord==2.4.0
# Phase 4A: Health Monitoring dependencies
fastapi==0.104.1
uvicorn==0.24.0
psutil==5.9.6
# Phase 4D: Testing dependencies - httpx managed by supabase
# httpx will be installed automatically as supabase dependency